import React from 'react'

const Form = () => {

    return (
        <form>
            <label>Email</label>
            <input type="text" placeholder="digite seu email" />
            <button>Agendar</button>
        </form>
    )
}

export default Form